import React from 'react';
import {Box} from "@material-ui/core";<% if(features.includes('styles')) { %>
import {makeStyles} from "@material-ui/core/styles";

import {styles} from './styles';<% } %>
<% if(features.includes('propTypes')) { %>
interface <%=name%>Props {

}<% } %>
<% if(features.includes('styles')) { %>
const useStyles = makeStyles(styles);
<% } %>
const <%=name%> = (<% if (features.includes('propTypes')) { %>props: <%=name%>Props<% } %>) => {
<% if(features.includes('styles')) { %>  const classes = useStyles();
<% } %>
  return (
    <Box<% if(features.includes('styles')) { %> className={classes.<%=className%>} <% } %>>

    </Box>
  );
};

export default <%=name%>;